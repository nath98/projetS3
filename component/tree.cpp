#include "tree.h"
/*
Tree::Tree() : m_initial_Node(NULL, "/home"){
	m_current_node = &m_initial_Node;
}


Tree::~Tree(){

}

string Tree::get_path(){
	Node* node_ptr = m_current_node;
	string path(node_ptr->get_name());
	while(node_ptr = node_ptr->get_predecessor()){
		path = node_ptr->get_name() + "/" + path;
	}
}


void Tree::go_to(string path){
	if(path[0] == '/'){
		m_node = m_initial_Node;
	}
	stringstream ss(m_message);
	std::vector<std::string> tableau_sous_chaine;
	std::string sous_chaine;
	int j = 0;
	while(getline(ss, sous_chaine, ' ')){
		tableau_sous_chaine.push_back(sous_chaine);
		}
}
*/
